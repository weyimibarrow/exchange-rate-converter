flask>=3.1.3
requests>=2.32
# Pinned to a patched release (fixes several Jinja2 CVEs present in 3.1.2).
jinja2>=3.1.6
# Transitive deps of requests, pinned to patched releases.
urllib3>=2.7.0
idna>=3.15
